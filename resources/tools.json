[
  {
    "type": "function",
    "function": {
      "name": "fetch_hype_candles",
      "description": "Fetch HYPE candlestick (OHLCV) data from Hyperliquid. Use this to get price history for analysis.",
      "parameters": {
        "type": "object",
        "properties": {
          "lookback_days": {
            "type": "integer",
            "description": "Number of days of historical data to fetch. Default 5.",
            "default": 5
          },
          "interval": {
            "type": "string",
            "description": "Candle interval. Options: 1m, 5m, 15m, 30m, 1h, 4h, 1d. Default 4h.",
            "default": "4h"
          }
        },
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "calculate_mfi",
      "description": "Calculate the Money Flow Index (MFI) for HYPE using the trailing N candles. Automatically fetches exactly the candles needed. Returns the MFI value and a BUY/SELL/NEUTRAL signal. MFI below 30 = BUY (oversold), above 70 = SELL (overbought).",
      "parameters": {
        "type": "object",
        "properties": {
          "interval": {
            "type": "string",
            "description": "Candle interval. Options: 1m, 5m, 15m, 30m, 1h, 4h, 1d. Default 4h.",
            "default": "4h"
          },
          "period": {
            "type": "integer",
            "description": "Number of trailing candles to use for the MFI calculation. Default 10.",
            "default": 10
          }
        },
        "required": []
      }
    }
  }
]
